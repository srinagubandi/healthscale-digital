<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Details - Health Scale Digital</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background: #f5f7fa;
            min-height: 100vh;
        }
        
        .dashboard-header {
            background: #1470AF;
            color: white;
            padding: 20px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .dashboard-header h1 {
            font-size: 20px;
            font-weight: 600;
        }
        
        .btn-back {
            background: rgba(255,255,255,0.2);
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            text-decoration: none;
            font-size: 14px;
            transition: background 0.2s;
        }
        
        .btn-back:hover {
            background: rgba(255,255,255,0.3);
        }
        
        .content {
            padding: 30px 40px;
            max-width: 900px;
            margin: 0 auto;
        }
        
        .card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            margin-bottom: 24px;
            overflow: hidden;
        }
        
        .card-header {
            padding: 20px 24px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .card-header h2 {
            font-size: 18px;
            color: #333;
        }
        
        .card-body {
            padding: 24px;
        }
        
        .detail-row {
            display: grid;
            grid-template-columns: 140px 1fr;
            gap: 16px;
            padding: 12px 0;
            border-bottom: 1px solid #f0f0f0;
        }
        
        .detail-row:last-child {
            border-bottom: none;
        }
        
        .detail-label {
            font-weight: 600;
            color: #666;
            font-size: 14px;
        }
        
        .detail-value {
            color: #333;
            font-size: 14px;
        }
        
        .detail-value a {
            color: #1470AF;
            text-decoration: none;
        }
        
        .detail-value a:hover {
            text-decoration: underline;
        }
        
        .message-content {
            background: #f9fafb;
            padding: 16px;
            border-radius: 8px;
            white-space: pre-wrap;
            line-height: 1.6;
        }
        
        .status-select {
            padding: 8px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 6px;
            font-size: 14px;
            font-family: inherit;
            cursor: pointer;
        }
        
        .status-select:focus {
            outline: none;
            border-color: #1470AF;
        }
        
        .notification-log {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 0;
            border-bottom: 1px solid #f0f0f0;
        }
        
        .notification-log:last-child {
            border-bottom: none;
        }
        
        .log-icon {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
        }
        
        .log-icon.email {
            background: #e3f2fd;
        }
        
        .log-icon.whatsapp {
            background: #e8f5e9;
        }
        
        .log-details {
            flex: 1;
        }
        
        .log-type {
            font-weight: 600;
            font-size: 14px;
            color: #333;
        }
        
        .log-status {
            font-size: 12px;
            color: #666;
        }
        
        .log-status.sent {
            color: #2e7d32;
        }
        
        .log-status.failed {
            color: #c62828;
        }
        
        .log-time {
            font-size: 12px;
            color: #999;
        }
        
        .action-buttons {
            display: flex;
            gap: 12px;
            margin-top: 24px;
        }
        
        .btn {
            padding: 10px 20px;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 500;
            text-decoration: none;
            cursor: pointer;
            border: none;
            transition: all 0.2s;
        }
        
        .btn-primary {
            background: #1470AF;
            color: white;
        }
        
        .btn-primary:hover {
            background: #0d5a8c;
        }
        
        .btn-danger {
            background: #dc3545;
            color: white;
        }
        
        .btn-danger:hover {
            background: #c82333;
        }
        
        @media (max-width: 768px) {
            .content {
                padding: 20px;
            }
            
            .detail-row {
                grid-template-columns: 1fr;
                gap: 4px;
            }
        }
    </style>
</head>
<body>
    <header class="dashboard-header">
        <h1>📧 Submission Details</h1>
        <a href="/admin" class="btn-back">← Back to Dashboard</a>
    </header>
    
    <main class="content">
        <div class="card">
            <div class="card-header">
                <h2>Contact Information</h2>
                <select class="status-select" onchange="updateStatus(this.value)">
                    <option value="new" <%= submission.status === 'new' ? 'selected' : '' %>>New</option>
                    <option value="read" <%= submission.status === 'read' ? 'selected' : '' %>>Read</option>
                    <option value="replied" <%= submission.status === 'replied' ? 'selected' : '' %>>Replied</option>
                    <option value="archived" <%= submission.status === 'archived' ? 'selected' : '' %>>Archived</option>
                </select>
            </div>
            <div class="card-body">
                <div class="detail-row">
                    <span class="detail-label">Name</span>
                    <span class="detail-value"><%= submission.name %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Email</span>
                    <span class="detail-value">
                        <a href="mailto:<%= submission.email %>"><%= submission.email %></a>
                    </span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Phone</span>
                    <span class="detail-value">
                        <% if (submission.phone) { %>
                            <a href="tel:<%= submission.phone %>"><%= submission.phone %></a>
                        <% } else { %>
                            Not provided
                        <% } %>
                    </span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Company</span>
                    <span class="detail-value"><%= submission.company || 'Not provided' %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Submitted</span>
                    <span class="detail-value"><%= new Date(submission.created_at).toLocaleString() %></span>
                </div>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h2>Message</h2>
            </div>
            <div class="card-body">
                <div class="message-content"><%= submission.message %></div>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h2>Notification Log</h2>
            </div>
            <div class="card-body">
                <% if (logs.length === 0) { %>
                    <p style="color: #666; font-size: 14px;">No notifications sent yet.</p>
                <% } else { %>
                    <% logs.forEach(log => { %>
                        <div class="notification-log">
                            <div class="log-icon <%= log.type %>">
                                <%= log.type === 'email' ? '📧' : '📱' %>
                            </div>
                            <div class="log-details">
                                <div class="log-type"><%= log.type === 'email' ? 'Email' : 'WhatsApp' %> Notification</div>
                                <div class="log-status <%= log.status %>"><%= log.status === 'sent' ? '✓ Sent successfully' : '✗ ' + log.details %></div>
                            </div>
                            <div class="log-time"><%= new Date(log.created_at).toLocaleString() %></div>
                        </div>
                    <% }) %>
                <% } %>
            </div>
        </div>
        
        <div class="action-buttons">
            <a href="mailto:<%= submission.email %>?subject=Re: Your inquiry to Health Scale Digital" class="btn btn-primary">
                Reply via Email
            </a>
            <button class="btn btn-danger" onclick="deleteSubmission()">Delete Submission</button>
        </div>
    </main>
    
    <script>
        async function updateStatus(status) {
            try {
                const response = await fetch('/admin/submission/<%= submission.id %>/status', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ status })
                });
                
                if (!response.ok) {
                    alert('Failed to update status');
                }
            } catch (error) {
                alert('Error updating status');
            }
        }
        
        async function deleteSubmission() {
            if (!confirm('Are you sure you want to delete this submission?')) return;
            
            try {
                const response = await fetch('/admin/submission/<%= submission.id %>', {
                    method: 'DELETE'
                });
                
                if (response.ok) {
                    window.location.href = '/admin';
                } else {
                    alert('Failed to delete submission');
                }
            } catch (error) {
                alert('Error deleting submission');
            }
        }
    </script>
</body>
</html>
